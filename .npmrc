# @nexis scope is on the public npm registry (no custom registry needed).
# For local publish: `npm login` or set NPM_TOKEN.
# This file is kept for reference; public installs use https://registry.npmjs.org/ by default.
@nexis:registry=https://registry.npmjs.org/
