# Route the @mohammedaydan scope to GitHub Packages.
# Authentication is intentionally NOT stored here:
# - Local dev: configure your token once in ~/.npmrc or set GITHUB_TOKEN
#   (see .npmrc.example).
# - CI: GitHub writes an isolated npmrc from secrets.GITHUB_TOKEN.
@mohammedaydan:registry=https://npm.pkg.github.com
