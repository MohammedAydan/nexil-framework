{
  "name": "@nexis/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "nexis": "dist/bin.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && printf '#!/usr/bin/env node\\nimport { parseCommand, helpText } from \"./index.js\";\\nconsole.log(helpText());\\n' > dist/bin.js",
    "test": "vitest run"
  }
}
