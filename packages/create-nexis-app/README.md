# create-nexis-app (superseded)

> **This package is superseded by [`@nexis/create-nexis`](../create-nexis) and is no longer published.**
> It is kept in the monorepo for reference only.

Use the canonical initializer instead:

```powershell
pnpm dlx @nexis/create-nexis my-nexis-app --yes --ts
```
