/** Production-only direct-DOM bindings runtime. Link navigation disposes the previous outlet bindings, swaps #app, then refreshes this delegated registry. */
export const RESUMABILITY_BINDINGS_EXTERNAL = `(()=>{const valid=/^nx:(?:signal|store):[A-Za-z0-9_-]+$/,registry=globalThis.__nexisScopeRegistry||(globalThis.__nexisScopeRegistry=new Map);let disposers=[];const signal=initial=>{const listeners=new Set,read=()=>initial;read.set=next=>{next=typeof next==='function'?next(initial):next;if(Object.is(initial,next))return;initial=next;for(const listener of[...listeners])listener()};read.subscribe=listener=>(listeners.add(listener),()=>listeners.delete(listener));return read},owner=element=>{while(element&&element!==document&&!element.hasAttribute('data-nx-scope'))element=element.parentElement;return element},scope=element=>{element=owner(element);if(!element)return{};const raw=element.getAttribute('data-nx-scope');let payload;try{payload=raw?.startsWith('nx:scope:')?globalThis.__nexisScopeSeeds?.[raw]:JSON.parse(raw)}catch{return{}}const resolved={};for(const[name,ref]of Object.entries(payload||{})){if(ref.kind==='signal'){let live=registry.get(ref.id);if(!live)registry.set(ref.id,live=signal(ref.initial));resolved[name]=registry.get(ref.id)}else if(ref.kind==='store'){let live=registry.get(ref.id);if(!live){const value=signal(ref.initial);live={value,g:ref.lifetime==='global'};registry.set(ref.id,live)}resolved[name]=registry.get(ref.id)}}return resolved},findSignal=(element,id)=>{const resolved=scope(element);for(const name in resolved){const value=resolved[name];if(value&&value.value&&typeof value.value.subscribe==='function'){if(value.value===registry.get(id)?.value)return value.value}else if(value===registry.get(id))return value}},apply=(element,target,value)=>{if(target==='text')element.textContent=value==null?'':String(value);else if(target==='value')element.value=value==null?'':String(value);else if(target==='checked')element.checked=!!value;else if(target==='disabled')element.disabled=!!value;else if(target==='hidden')element.hidden=!!value;else if(target==='class')element.className=value==null?'':String(value);else if(target==='style'){if(value&&typeof value==='object')Object.assign(element.style,value);else element.style.cssText=value==null?'':String(value)}else if(target==='href'||target==='src'){if(value==null||value==='')element.removeAttribute(target);else element.setAttribute(target,String(value))}else if(target.startsWith('aria-')){if(value==null)element.removeAttribute(target);else element.setAttribute(target,String(value))}},bind=()=>{for(const element of document.querySelectorAll('[data-nx-bind]')){const attribute=element.getAttribute('data-nx-bind');for(const reference of(attribute||'').split(';')){const marker=reference.lastIndexOf('#'),id=reference.slice(0,marker),target=reference.slice(marker+1),live=findSignal(element,id);if(marker<1||!valid.test(id)||(!['text','value','checked','disabled','hidden','class','style','href','src'].includes(target)&&!/^aria-[a-z][a-z0-9-]*$/.test(target))||typeof live!=='function'||typeof live.subscribe!=='function')continue;const render=()=>apply(element,target,live());render();disposers.push(live.subscribe(render))}}};globalThis.__nexisDisposeBindings=()=>{disposers.splice(0).forEach(dispose=>dispose());for(const[id,live]of registry)if(!live.g)registry.delete(id)};globalThis.__nexisRefreshBindings=bind;bind()})();\n`
