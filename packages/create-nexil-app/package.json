{
  "name": "@nexil/create-nexil-app",
  "version": "0.0.1",
  "private": true,
  "description": "[Superseded by @nexil/create-nexil] Create a Nexil application",
  "type": "module",
  "dependencies": {
    "@nexil/starter": "workspace:*"
  },
  "files": [
    "dist",
    "!dist/**/*.test.*",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MohammedAydan/nexil-framework.git",
    "directory": "packages/create-nexil-app"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "bin": {
    "create-nexil-app": "dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"const fs=require('fs');for(const f of ['dist','tsconfig.tsbuildinfo']){try{fs.rmSync(f,{recursive:true,force:true})}catch(e){}}\""
  }
}
