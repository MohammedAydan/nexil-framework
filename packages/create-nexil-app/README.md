# create-nexil-app (superseded)

> **This package is superseded by [`@nexil/create-nexil`](../create-nexil) and is no longer published.**
> It is kept in the monorepo for reference only.

Use the canonical initializer instead:

```powershell
pnpm dlx @nexil/create-nexil my-nexil-app --yes --ts
```
